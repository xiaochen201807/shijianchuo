# ============================================================
# 最终镜像构建 (linux/amd64 + linux/arm64)
# 镜像: ghcr.io/<owner>/<repo>/tsa
# 内容: Tongsuo TSA + nginx + chrony + tsa-demo 原生二进制 (无 JVM)
# 构建: 多阶段 Dockerfile (GraalVM native → 运行时)
# ============================================================
name: Docker Build & Push

on:
  push:
    branches: [main, master, develop]
    tags: ["v*"]
    paths:
      - "Dockerfile"
      - "docker/**"
      - "tsa-server/**"
      - "sdk/**"
      - "sdk-demo/**"
      - "pom.xml"
      - "docker-compose.yml"
      - ".github/workflows/docker-build.yml"
  pull_request:
    branches: [main, master, develop]
    paths:
      - "Dockerfile"
      - "docker/**"
      - "tsa-server/**"
      - "sdk/**"
      - "sdk-demo/**"
      - "pom.xml"
      - "docker-compose.yml"
      - ".github/workflows/docker-build.yml"
  workflow_dispatch:
    inputs:
      push_images:
        description: "是否推送到 GHCR（默认 true）"
        required: false
        default: "true"
        type: choice
        options:
          - "true"
          - "false"

concurrency:
  group: docker-build-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  ALIYUN_REGISTRY: registry.cn-hangzhou.aliyuncs.com
  ALIYUN_NAMESPACE: shinyue
  # 单镜像名
  IMAGE_NAME: tsa

jobs:
  prepare:
    name: Prepare
    runs-on: ubuntu-latest
    outputs:
      push: ${{ steps.meta.outputs.push }}
      image: ${{ steps.meta.outputs.image }}
      aliyun_image: ${{ steps.meta.outputs.aliyun_image }}
    steps:
      - name: Meta
        id: meta
        run: |
          OWNER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          REPO=$(echo "${{ github.event.repository.name }}" | tr '[:upper:]' '[:lower:]')
          IMAGE="ghcr.io/${OWNER}/${REPO}/${{ env.IMAGE_NAME }}"
          ALIYUN_IMAGE="${{ env.ALIYUN_REGISTRY }}/${{ env.ALIYUN_NAMESPACE }}/${{ env.IMAGE_NAME }}"
          echo "image=${IMAGE}" >> "$GITHUB_OUTPUT"
          echo "aliyun_image=${ALIYUN_IMAGE}" >> "$GITHUB_OUTPUT"

          PUSH="false"
          if [ "${{ github.event_name }}" = "push" ]; then
            PUSH="true"
          elif [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ "${{ inputs.push_images }}" = "true" ]; then
            PUSH="true"
          fi
          echo "push=${PUSH}" >> "$GITHUB_OUTPUT"
          echo "GHCR Image:   ${IMAGE}"
          echo "Aliyun Image: ${ALIYUN_IMAGE}"
          echo "Push:         ${PUSH}"
          echo "Arch:         linux/amd64 + linux/arm64"

  build:
    name: Build (${{ matrix.platform }})
    needs: prepare
    runs-on: ${{ matrix.runner }}
    # Tongsuo 编译 + GraalVM native-image，耗时较长
    timeout-minutes: 180
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux/amd64
            runner: ubuntu-latest
          - platform: linux/arm64
            runner: ubuntu-24.04-arm
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Platform pair
        id: prep
        run: |
          echo "platform_pair=${PLATFORM//\//-}" >> "$GITHUB_OUTPUT"
        env:
          PLATFORM: ${{ matrix.platform }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        if: needs.prepare.outputs.push == 'true'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to Aliyun Registry
        if: needs.prepare.outputs.push == 'true'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.ALIYUN_REGISTRY }}
          username: ${{ secrets.ALIYUN_REGISTRY_USER }}
          password: ${{ secrets.ALIYUN_REGISTRY_PASSWORD }}

      - name: Docker meta (labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ needs.prepare.outputs.image }}
            ${{ needs.prepare.outputs.aliyun_image }}
          labels: |
            org.opencontainers.image.title=tsa-all-in-one
            org.opencontainers.image.description=RFC 3161 GM TSA (GmSSL3 + nginx + fcgiwrap + chrony)

      - name: Build and push by digest
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: ${{ matrix.platform }}
          labels: |
            ${{ steps.meta.outputs.labels }}
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.description=RFC3161 GM TSA + native tsa-demo (no JVM)
          outputs: ${{ needs.prepare.outputs.push == 'true' && format('type=image,"name={0},{1}",push-by-digest=true,name-canonical=true,push=true', needs.prepare.outputs.image, needs.prepare.outputs.aliyun_image) || 'type=cacheonly' }}
          cache-from: type=gha,scope=tsa-final-${{ steps.prep.outputs.platform_pair }}
          cache-to: type=gha,mode=max,scope=tsa-final-${{ steps.prep.outputs.platform_pair }}
          provenance: false
          sbom: false

      - name: Export digest
        if: needs.prepare.outputs.push == 'true'
        run: |
          mkdir -p /tmp/digests
          DIGEST="${{ steps.build.outputs.digest }}"
          test -n "$DIGEST"
          touch "/tmp/digests/${DIGEST#sha256:}"
          ls -la /tmp/digests

      - name: Upload digest
        if: needs.prepare.outputs.push == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: digests-tsa-${{ steps.prep.outputs.platform_pair }}
          path: /tmp/digests/*
          if-no-files-found: error
          retention-days: 1

  merge:
    name: Merge multi-arch
    needs: [prepare, build]
    if: needs.prepare.outputs.push == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Download digests
        uses: actions/download-artifact@v4
        with:
          pattern: digests-tsa-*
          path: /tmp/digests
          merge-multiple: true

      - name: List digests
        run: |
          ls -la /tmp/digests
          test "$(find /tmp/digests -type f | wc -l)" -ge 1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to Aliyun Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.ALIYUN_REGISTRY }}
          username: ${{ secrets.ALIYUN_REGISTRY_USER }}
          password: ${{ secrets.ALIYUN_REGISTRY_PASSWORD }}

      - name: Extract GHCR tags
        id: meta_ghcr
        uses: docker/metadata-action@v5
        with:
          images: ${{ needs.prepare.outputs.image }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=sha,prefix=sha-,format=short
            type=raw,value=build-${{ github.run_number }},enable=${{ github.event_name == 'workflow_dispatch' }}

      - name: Extract Aliyun tags
        id: meta_aliyun
        uses: docker/metadata-action@v5
        with:
          images: ${{ needs.prepare.outputs.aliyun_image }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=sha,prefix=sha-,format=short
            type=raw,value=build-${{ github.run_number }},enable=${{ github.event_name == 'workflow_dispatch' }}

      - name: Create multi-arch manifest
        working-directory: /tmp/digests
        run: |
          set -euo pipefail

          # 1. GHCR Manifest
          IMAGE="${{ needs.prepare.outputs.image }}"
          GHCR_DIGEST_REFS=()
          for f in *; do
            [ -f "$f" ] || continue
            GHCR_DIGEST_REFS+=("${IMAGE}@sha256:${f}")
          done
          printf 'GHCR Digest Refs:\n'; printf '  %s\n' "${GHCR_DIGEST_REFS[@]}"

          GHCR_TAG_ARGS=()
          while IFS= read -r tag; do
            [ -n "$tag" ] || continue
            GHCR_TAG_ARGS+=("-t" "${tag}")
          done <<< "${{ steps.meta_ghcr.outputs.tags }}"

          echo "Creating GHCR multi-arch manifest..."
          docker buildx imagetools create "${GHCR_TAG_ARGS[@]}" "${GHCR_DIGEST_REFS[@]}"

          # 2. Aliyun Registry Manifest
          ALIYUN_IMAGE="${{ needs.prepare.outputs.aliyun_image }}"
          ALIYUN_DIGEST_REFS=()
          for f in *; do
            [ -f "$f" ] || continue
            ALIYUN_DIGEST_REFS+=("${ALIYUN_IMAGE}@sha256:${f}")
          done
          printf 'Aliyun Digest Refs:\n'; printf '  %s\n' "${ALIYUN_DIGEST_REFS[@]}"

          ALIYUN_TAG_ARGS=()
          while IFS= read -r tag; do
            [ -n "$tag" ] || continue
            ALIYUN_TAG_ARGS+=("-t" "${tag}")
          done <<< "${{ steps.meta_aliyun.outputs.tags }}"

          echo "Creating Aliyun multi-arch manifest..."
          docker buildx imagetools create "${ALIYUN_TAG_ARGS[@]}" "${ALIYUN_DIGEST_REFS[@]}"

      - name: Inspect
        run: |
          FIRST_GHCR=$(echo "${{ steps.meta_ghcr.outputs.tags }}" | head -n1)
          docker buildx imagetools inspect "${FIRST_GHCR}" || true
          {
            echo "## All-in-One multi-arch image"
            echo ""
            echo "- **GHCR Image**: \`${{ needs.prepare.outputs.image }}\`"
            echo "- **Aliyun Image**: \`${{ needs.prepare.outputs.aliyun_image }}\`"
            echo "- **Platforms**: \`linux/amd64\`, \`linux/arm64\`"
            echo "- **Contains**: GmSSL3 + nginx + fcgiwrap + chrony"
            echo "- **Tags**:"
            echo '```'
            echo "${{ steps.meta_ghcr.outputs.tags }}"
            echo "${{ steps.meta_aliyun.outputs.tags }}"
            echo '```'
            echo ""
            echo '```bash'
            echo "# GHCR"
            echo "docker pull ${{ needs.prepare.outputs.image }}:latest"
            echo "# Aliyun"
            echo "docker pull ${{ needs.prepare.outputs.aliyun_image }}:latest"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

  summary:
    name: Summary
    needs: [prepare, build, merge]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Summary
        run: |
          {
            echo "## Docker All-in-One Build"
            echo ""
            echo "| Item | Value |"
            echo "|------|-------|"
            echo "| GHCR Image | \`${{ needs.prepare.outputs.image }}\` |"
            echo "| Aliyun Image | \`${{ needs.prepare.outputs.aliyun_image }}\` |"
            echo "| Platforms | amd64 + arm64 |"
            echo "| Push | \`${{ needs.prepare.outputs.push }}\` |"
            echo "| Build | \`${{ needs.build.result }}\` |"
            echo "| Merge | \`${{ needs.merge.result }}\` |"
          } >> "$GITHUB_STEP_SUMMARY"
